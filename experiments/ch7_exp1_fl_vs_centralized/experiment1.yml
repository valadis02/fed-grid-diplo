# =============================================================
# Πείραμα 1: Federated Learning με 5 Edge Nodes (RPi5 limits)
# Εκτέλεση: docker-compose -f experiment1.yml up
# =============================================================

networks:
  aqtf_shared_network:
    external: true

# =============================================================
# CLOUD
# =============================================================
services:

  cloud_app:
    image: cloud_node_app:latest
    container_name: exp1_cloud_app
    environment:
      - RABBITMQ_HOST=exp1_rabbitmq_cloud
      - CLOUD_RABBITMQ_HOST=exp1_rabbitmq_cloud
      - CLOUD_MQTT_HOST=exp1_mqtt_cloud
      - CLOUD_MQTT_PORT=1883
      - APP_HOST_PORT=8081
      - FOG_NAMES=FOG_NODE_1
      - NUM_FOG_NODES=1
    ports:
      - "8082:8081"
    depends_on:
      - exp1_rabbitmq_cloud
      - exp1_mqtt_cloud
    networks:
      - aqtf_shared_network
    command: python3 -m cloud.communication.cloud_messaging
    volumes:
      - ./cloud/models:/app/models
      - ./cloud/status:/app/status
      - ./data:/app/data:ro
    restart: on-failure

  exp1_rabbitmq_cloud:
    image: rabbitmq:3-management
    container_name: rabbitmq_cloud
    ports:
      - "5673:5672"
      - "15673:15672"
    networks:
      - aqtf_shared_network

  exp1_mqtt_cloud:
    image: eclipse-mosquitto
    container_name: mqtt_cloud
    ports:
      - "1884:1883"
    networks:
      - aqtf_shared_network
    volumes:
      - ./cloud/mosquitto.conf:/mosquitto/config/mosquitto.conf

# =============================================================
# FOG
# =============================================================

  fog_app:
    image: fog_node_app:latest
    container_name: exp1_fog_app
    environment:
      - RABBITMQ_HOST=exp1_rabbitmq_fog
      - FOG_HOST=exp1_rabbitmq_fog
      - FOG_RABBITMQ_HOST=exp1_rabbitmq_fog
      - CLOUD_HOST=exp1_rabbitmq_cloud
      - CLOUD_RABBITMQ_HOST=exp1_rabbitmq_cloud
      - FOG_MQTT_HOST=exp1_mqtt_fog
      - FOG_MQTT_PORT=1883
      - CLOUD_MQTT_HOST=exp1_mqtt_cloud
      - CLOUD_MQTT_PORT=1883
      - FOG_NAME=FOG_NODE_1
      - EDGE_NAMES=edge_node_1,edge_node_2,edge_node_3,edge_node_4,edge_node_5
      - APP_HOST_PORT=8081
    ports:
      - "8084:8081"
    depends_on:
      - exp1_rabbitmq_fog
      - exp1_mqtt_fog
      - exp1_rabbitmq_cloud
    networks:
      - aqtf_shared_network
    command: uvicorn fog.communication.app_factory:app --host 0.0.0.0 --port 8081
    volumes:
      - ./fog/models:/app/models
      - ./fog/models/outbox:/app/models/outbox
    restart: on-failure

  exp1_rabbitmq_fog:
    image: rabbitmq:3-management
    container_name: rabbitmq_fog
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - aqtf_shared_network

  exp1_mqtt_fog:
    image: eclipse-mosquitto
    container_name: mqtt_fog
    ports:
      - "1883:1883"
    networks:
      - aqtf_shared_network

# =============================================================
# EDGE NODES (x5) — RPi5 resource limits
# =============================================================

  edge_node_1:
    image: edge-node:latest
    container_name: exp1_edge_1
    environment:
      - FOG_RABBITMQ_HOST=exp1_rabbitmq_fog
      - FOG_MQTT_HOST=exp1_mqtt_fog
      - FOG_MQTT_PORT=1883
      - EDGE_NAME=edge_node_1
      - MAX_ROUNDS=10
      - WAIT_TIMEOUT_SECS=300
      - TF_CPP_MIN_LOG_LEVEL=2
      - TF_ENABLE_ONEDNN_OPTS=0
    depends_on:
      - exp1_rabbitmq_fog
      - fog_app
    networks:
      - aqtf_shared_network
    command: python3 -m edge.model.run_federated_cycle
    volumes:
      - ./data/house_1:/app/edge/data
      - ./edge/models/node_1:/app/edge/models
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8g
    restart: on-failure

  edge_node_2:
    image: edge-node:latest
    container_name: exp1_edge_2
    environment:
      - FOG_RABBITMQ_HOST=exp1_rabbitmq_fog
      - FOG_MQTT_HOST=exp1_mqtt_fog
      - FOG_MQTT_PORT=1883
      - EDGE_NAME=edge_node_2
      - MAX_ROUNDS=10
      - WAIT_TIMEOUT_SECS=300
      - TF_CPP_MIN_LOG_LEVEL=2
      - TF_ENABLE_ONEDNN_OPTS=0
    depends_on:
      - exp1_rabbitmq_fog
      - fog_app
    networks:
      - aqtf_shared_network
    command: python3 -m edge.model.run_federated_cycle
    volumes:
      - ./data/house_2:/app/edge/data
      - ./edge/models/node_2:/app/edge/models
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8g
    restart: on-failure

  edge_node_3:
    image: edge-node:latest
    container_name: exp1_edge_3
    environment:
      - FOG_RABBITMQ_HOST=exp1_rabbitmq_fog
      - FOG_MQTT_HOST=exp1_mqtt_fog
      - FOG_MQTT_PORT=1883
      - EDGE_NAME=edge_node_3
      - MAX_ROUNDS=10
      - WAIT_TIMEOUT_SECS=300
      - TF_CPP_MIN_LOG_LEVEL=2
      - TF_ENABLE_ONEDNN_OPTS=0
    depends_on:
      - exp1_rabbitmq_fog
      - fog_app
    networks:
      - aqtf_shared_network
    command: python3 -m edge.model.run_federated_cycle
    volumes:
      - ./data/house_3:/app/edge/data
      - ./edge/models/node_3:/app/edge/models
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8g
    restart: on-failure

  edge_node_4:
    image: edge-node:latest
    container_name: exp1_edge_4
    environment:
      - FOG_RABBITMQ_HOST=exp1_rabbitmq_fog
      - FOG_MQTT_HOST=exp1_mqtt_fog
      - FOG_MQTT_PORT=1883
      - EDGE_NAME=edge_node_4
      - MAX_ROUNDS=10
      - WAIT_TIMEOUT_SECS=300
      - TF_CPP_MIN_LOG_LEVEL=2
      - TF_ENABLE_ONEDNN_OPTS=0
    depends_on:
      - exp1_rabbitmq_fog
      - fog_app
    networks:
      - aqtf_shared_network
    command: python3 -m edge.model.run_federated_cycle
    volumes:
      - ./data/house_4:/app/edge/data
      - ./edge/models/node_4:/app/edge/models
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8g
    restart: on-failure

  edge_node_5:
    image: edge-node:latest
    container_name: exp1_edge_5
    environment:
      - FOG_RABBITMQ_HOST=exp1_rabbitmq_fog
      - FOG_MQTT_HOST=exp1_mqtt_fog
      - FOG_MQTT_PORT=1883
      - EDGE_NAME=edge_node_5
      - MAX_ROUNDS=10
      - WAIT_TIMEOUT_SECS=300
      - TF_CPP_MIN_LOG_LEVEL=2
      - TF_ENABLE_ONEDNN_OPTS=0
    depends_on:
      - exp1_rabbitmq_fog
      - fog_app
    networks:
      - aqtf_shared_network
    command: python3 -m edge.model.run_federated_cycle
    volumes:
      - ./data/house_5:/app/edge/data
      - ./edge/models/node_5:/app/edge/models
    deploy:
      resources:
        limits:
          cpus: '4.0'
          memory: 8g
    restart: on-failure